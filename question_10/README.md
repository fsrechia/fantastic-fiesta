cleanup, compile and run with:

    rm -vf a.out && g++ main.cpp && ./a.out

Enter a string and press <enter> to get the length of it. Example:

    rechia@DESKTOP-F1P7VPK:~/fantastic-fiesta/question_10$ rm -vf a.out && g++ main.cpp && ./a.out
    removed 'a.out'
    123
    length is: 3